# Recursive Reverse String

This project prints a sentence in reverse order using a recursive function that starts from the last character and works backwards to the first.

## What I Learned
- How to reverse print a string using recursion
- How to use string length to determine the starting index
- How each recursive call moves one character closer to the beginning
